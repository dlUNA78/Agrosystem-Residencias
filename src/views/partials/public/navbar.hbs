<nav class="bg-transparent fixed top-0 w-full z-50">
    <div
        class="flex justify-between items-center px-6 md:px-10 py-6 max-w-[1440px] mx-auto font-body tracking-tight antialiased">
        <div class="flex items-center gap-12">
            <span class="text-2xl font-headline font-bold text-white tracking-widest uppercase">Agrosystem INIFAP</span>

            <div class="hidden md:flex items-center gap-8">
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 {{#if (eq activePage 'home')}}text-white border-b-2 border-white{{else}}text-white/70 hover:text-white border-b-2 border-transparent hover:border-white/40{{/if}}"
                    href="/">Inicio</a>
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 {{#if (eq activePage 'plagues')}}text-white border-b-2 border-white{{else}}text-white/70 hover:text-white border-b-2 border-transparent hover:border-white/40{{/if}}"
                    href="/plagues">Plagas</a>
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 {{#if (eq activePage 'crops')}}text-white border-b-2 border-white{{else}}text-white/70 hover:text-white border-b-2 border-transparent hover:border-white/40{{/if}}"
                    href="/crops">Cultivos</a>
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 {{#if (eq activePage 'products')}}text-white border-b-2 border-white{{else}}text-white/70 hover:text-white border-b-2 border-transparent hover:border-white/40{{/if}}"
                    href="/products">Productos</a>
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 {{#if (eq activePage 'forum')}}text-white border-b-2 border-white{{else}}text-white/70 hover:text-white border-b-2 border-transparent hover:border-white/40{{/if}}"
                    href="/forum">Foro</a>
               


                {{#if user}}
                {{#if (eq user.role 'inifap')}}
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 text-[#43655c] hover:text-white"
                    href="/dashboard">Ir al Panel INIFAP</a>
                {{/if}}
                {{#if (eq user.role 'admin')}}
                <a class="text-xs uppercase tracking-widest font-bold transition-all duration-200 pb-1 text-[#43655c] hover:text-white"
                    href="/dashboard">Ir al Panel INIFAP</a>
                {{/if}}
                {{/if}}
            </div>
        </div>

        <div class="flex items-center gap-2 md:gap-4">
            {{#if user}}
            <button class="p-2 text-white hover:bg-white/10 rounded-full transition-all">
                <span class="material-symbols-outlined">notifications</span>
            </button>
            <div class="h-8 w-8 rounded-full bg-white/20 mx-1 hidden sm:block"></div>
            <div class="hidden sm:flex items-center gap-3 pl-2">
                <div class="text-right hidden sm:block text-white">
                    <p class="text-xs font-bold leading-none">{{user.full_name}}</p>
                    <p class="text-[10px] text-white/70 capitalize">{{user.role}}</p>
                </div>
                {{#if user.photo_url}}
                <img alt="User avatar" class="w-10 h-10 rounded-full object-cover ring-2 ring-white/30"
                    src="{{user.photo_url}}" />
                {{else}}
                <div class="w-10 h-10 rounded-full bg-[#43655c] text-white flex items-center justify-center font-bold">
                    {{!-- Letra inicial dummy hasta que hagamos la extracción JS --}} U
                </div>
                {{/if}}
            </div>
            {{else}}
            <div class="hidden sm:flex gap-3">
                <a href="/auth/login"
                    class="text-sm font-bold text-white/90 hover:text-white transition-colors px-3 py-2">Ingresar</a>
                <a href="/auth/register"
                    class="text-sm font-bold text-black bg-white hover:bg-gray-200 rounded-none px-6 py-2 transition-all uppercase tracking-widest">Registrarse</a>
            </div>
            {{/if}}

            <button id="public-nav-toggle"
                class="md:hidden p-2 text-white hover:bg-white/10 rounded-lg transition-all duration-200"
                aria-label="Abrir menú de navegación" aria-expanded="false">
                <span class="material-symbols-outlined" id="public-nav-icon">menu</span>
            </button>
        </div>
    </div>

</nav>